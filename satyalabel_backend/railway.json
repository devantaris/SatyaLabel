{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.prod"
  },
  "deploy": {
    "startCommand": "sh -c \"python -m alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port ${PORT} --workers 2\""
  }
}
